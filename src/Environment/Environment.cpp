Environment::Environment() {

}

int Environment::update() {
	//go through objects and update
	/*
	BOOST_FOREACH( Object_Type * t, objects )
   	{
        	//call each objects decide method.
		//effect changes on all objects as necessary
   	}
	*/
	return 0;
}

int Environment::addObject(/*Object_Type * object*/) {
	//objects.push_back(object);
}

int Environment::removeObject(/*Object_Type * object*/) {
	/*
	for (iter = objects.begin(); iter != objects.end(); iter++) {
		if (*iter == object) {
			objects.erase(iter);
			return 0;
		}
	}
	return 1;
	*/
}
